# HTTP 长连接

HTTP 协议如果是短连接的话意味着每次连接之前都要建立 TCP 连接，完成请求之后释放 TCP 连接。建立连接需要三次握手，一来一去延迟是比较大的，而且 TCP 是慢启动的，传输开始阶段比较慢，每次新建 TCP 都需要慢启动挺影响延迟的。保持长连接的话意味着实现 **TCP 连接复用**，前一次请求完的 HTTP 的连接可以给后一个 HTTP 用，这将大大减少 TCP 三次握手和慢启动带来的延迟。不过 **TCP Fast Open** 可以解决三次握手带来延迟，可以了解下。
